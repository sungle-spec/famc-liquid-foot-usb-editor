# LF+ Editor v6.31 — control tooltips (extracted from the original app)

Every hover tooltip in the original FAMC LF+ Editor, extracted verbatim from the
compiled app, in the app's own layout order. Each entry lists the control id(s)
that show that tooltip. Generated by `scripts/extract_tooltips.py` — third-party
reference (FAMC's text), kept for interoperability/preservation.

**253 unique tooltips** across 387 control instances.

- Click to Load the associated items into the search/order list. Double-Click to change the editor mode.
  <br>· _`TabPanelQuickListType1`, `PushButtonOpen`, `TextFieldSearch`_
- Are we properly connected to the LF+
  <br>· _`TabPanelQuickListType11`_
- Enter text to start the search for the first match.  Press the Magnifier search icon to continue finding the next available match(s)
  <br>· _`UI_Button_WindowSelect`_
- Exposes or Hides the Search/Selector Quick Pick sytem
  <br>· _`UI_Button_WindowSelect`_
- if using Kemper, which mode is the kemper in? Performance mode can import preset data, or convert Performance names to Songs, and program the 5 presets per performance to Presets.
  <br>· _`PopupKemper_StartPreset`_
- Select the first PRESET (or PERFORMANCE) that you would like to start importing. NOTE THAT YOU CAN ONLY IMPORT AT MOST 384 Presets (or Performances)
  <br>· _`Label2`_
- Select the last PRESET (or PERFORMANCE) that you would like to start importing. NOTE THAT YOU CAN ONLY IMPORT AT MOST 384 Presets (or Performances)
  <br>· _`Label2`_
- will update the LF+ Editor preset name with the external devices received preset name.
  <br>· _`label2`, `PopupAXE_StartPreset`_
- Will update the LF+ Editor Preset with the Initial IA-States that were received during the external sync. This uses the Preset's defined IA-Slot Map if not set to default.
  <br>· _`jmToggleButtonKemperImportPCmsgs`, `Label2`_
- will add a PC# midi message to the first programming slot of the preset as it is programmed. If set to Program Performance information in Songs, then it will store the PC# message within the song.
  <br>· _`Label2`_
- last preset to update using the external sync data.
  <br>· _`PopupMenuKemperLFStartPreset`, `Label2`, `txtAXELFLastPreset`, `PopupAXE_EndPreset`_
- Select the starting preset to load the Kemper presets/performance into. If loading to Songs, then select the first song to load into.
  <br>· _`Label2`, `txtKemperLFLastSong`_
- AXE3 COMPATIBLE: This will read all presets selected and then auto program your IA-Slots to include all used AXE-FX Effects found. This will find empty IA-Slots to populate in numeric order with exception of SCENE, LOOPER, TAP and TUNER. It will not overwrite used IA-Slots. POWERFUL TIME SAVER!
  <br>· _`txtAXEdetectedDevice`_
- Model Detected
  <br>· _`jmToggleButtonAXEModifyCC`_
- AXE-FX MODEL II and similar version prior to AXE-FX III have default assigned CC#'s for each effect. If the user changes those values, this Import can detect such changes and automatically re-program your IA-Slots for those effects to have the correct MIDI commands. Turn ON this paramaeter to have the Sync auto program your IA-Slots to match CC#'s you entered into the AXE-FX.  AXE-FX III does not require any midi programming in the LF+ and this will be ignored for AXE-FX III.
  <br>· _`label2`_
- How many scenes per AXE-FX3 preset should be used when transfering in Preset-> SONG mode. Each Song will be the AXE-FX preset, and the Sync will create an individual LF+ Preset for each Scene, up to the number selected here. Less scenes means more presets can be loaded into Songs. The max # of presets is 384.
  <br>· _`Label2`_
- Select the last PRESET that you would like to start importing. NOTE THAT YOU CAN ONLY IMPORT AT MOST 384 Presets
  <br>· _`jmToggleButtonAXEImportPCmsgs`_
- Will add a PC# midi message to the first programming slot of the preset as it is programmed.
  <br>· _`jmToggleButtonAXEImportName`_
- Select the first PRESET that you would like to start importing. NOTE THAT YOU CAN ONLY IMPORT AT MOST 384 Presets
  <br>· _`PopupAXEMode`_
- Select the type of Sync you would like to execute.
  <br>· _`PopupMenuAXELFStartSong`_
- Select the starting preset to load the AXE-FX Presets into. If loading to Songs, then select the first song in the LF+ to load into.
  <br>· _`PopupMenuAXELFStartPreset`_
- Select the starting preset to load the AXE-FX presetsinto. If loading to Songs, then select the first song to load into.
  <br>· _`label2`_
- Preset Full Name
  <br>· _`Txt_LCD1`, `Txt_LCD1_FullName`, `Txt_LCD1_NickName`, `LblmainlcdName`, `LblmainlcdNickName`_
- Save changes, and if presets are involved, update ALL songs to modify preset assignments to match the original presets new location.
  <br>· _`PushButtonSaveResync`_
- Send the current Item to the connected LF+
  <br>· _`PushButtonItemSend`, `PushButtonItemGet`_
- Get the current Item from the connected LF+
  <br>· _`PushButtonItemGet`, `PushButtonBulkSend`_
- Send ALL Presets to the LF+
  <br>· _`PushButtonBulkSend`_
- Get ALL Presets from the LF+
  <br>· _`PushButtonBulkGet`_
- Close this Editor Window.
  <br>· _`CanvasClose`, `PushButtonBulkGet`, `CanvasWinMove`_
- Click and drag to move this editor window to new location. Expand the size of the LF+ Editor screen to make room for more open editors and better positioning. The Editor will save the location of your layouts if you have the default setting (reset position and size) turned OFF.

  <br>· _`CanvasWinMove`, `CanvasClose`, `LabelGroupName`_
- when editor loads, search for LF+ and sync data from LF+ if found.
  <br>· _`GroupBox1`, `GroupBoxHEADER`, `LabelGroupName`, `Label30`, `RoundRectangle1`, `UI_GroupBox1`_
- If you want this preset to allow multiple presses, check this parameter. If you are using STEPS, then you need the preset to allow Multi-Press. If you want your presets to "reset" themselves by pressing them multiple times, then you must allow multi-press
  <br>· _`jmToggleButtonPresetMultiPress`_
- Resend the current value for each of the expression pedals. It will utilize the last value read by the expression pedal position. This only works for pedals set as Continous Controllers.
  <br>· _`jmToggleButtonPresetResendEXP`_
- If ON, replace the current state of all global IA-Slots with the values defined in the Initial States for this preset. Turn on Resend Initial Global Parameter if you want to also send these new states.
  <br>· _`jmToggleButtonPresetGlobalOverride`_
- Will send all Global IA-Slot commands (either ON or BYPASS) based on the current global state of that particular IA-Slot. It will NOT utilize the intiial IA-State value defined for this preset, unless the override parameter below is selected for this preset.
  <br>· _`jmToggleButtonPresetResendGlobal`_
- Will send all IA-Slot commands (either ON or BYPASS) based on the initial states defined for this Preset. Initial States are set to either (OFF, ON, BYPASS, or BLOCKED). OFF states will be resent if Global setting is set to allow OFF to be sent as BYPASS.
  <br>· _`jmToggleButtonPresetResendIA`_
- when preset loads, should we reset all Page button functions to the default function #1? if set, All button (that are NOT Preset holders) will be changed if function #2 is currently selected and active on the button. Effectively putting the system back into the default page order for non-preset buttons.
  <br>· _`jmToggleButtonPresetResetPageFunctions`_
- if external device sync parameters are set, this preset will try to re-sync with an external device to get current Sync'd IA-Slot States. All IA's that are properly sync'd will update their states and status to match the external device.
  <br>· _`jmToggleButtonPresetEnableExternalSync`_
- By default presets send pending SONG commands, then their programming commands, and then IA-Slots.  Turn this parameter ON to allow IA-Slots to process first, and then preset commands.
  <br>· _`jmToggleButtonProgrammingPost`_
- if properly set to sync with an external device- the Preset name will override and use the external devices current preset name
  <br>· _`jmToggleButtonPresetEnableUseExternalSyncPresetName`_
- when this preset loads, should we reset all Page button functions that currently set for Function #2 active back to Function #1 active. This will ONLY effect buttons that have at least one Preset Holder within the button.
  <br>· _`jmToggleButtonPresetResetPageFunctionsPreset`_
- OFF: Blocked IA-Slots are ignored and will NOT process any commands. When ON, Blocked IA's will initially trigger BYPASSED commands and then block the LF+ and the user from triggering the IA-Slot.
  <br>· _`jmToggleButtonPresetSendBlockedIAInitialState`_
- Page to Load when Preset is activated
  <br>· _`ComboBoxPresetPage`_
- IA-Map to use for the page used by this preset
  <br>· _`ComboBoxIAMap`_
- Will set the Page Start Button Value to Global Setting, Currently Active, or a specific button within the curent page. This value defines which virtual button (on current page) will be assigned to the first Physical button on the controller. So setting the value to 13, will map PAGE BUTTON 13 to physical button 1. all other buttons follow in sequential order.
  <br>· _`ComboBoxButtonStart`_
- Preset will trigger and process, then wait for you to release the button. Once released, the previous preset will "re-load". GLOBAL Setting "Block Preset SAVE" must be turned ON. Any momentary preset must be on a single function button to work properly.
  <br>· _`jmToggleButtonMomentary`_
- This will make the preset act like an IA-Slot is being pressed. It will not RESEND IA's, change pages or IA-Maps, or reset IA-Slots based on Initial IA States.
  <br>· _`jmToggleButtonPresetActAsIA`_
- OFF: Expression Pedals will remain in current state of programming    ON: When this preset is triggered, it will temporarily BLOCK the Expression pedal from transmitting until an unblock command is executed, or any of the EXP... programming commands are processed.
  <br>· _`jmToggleButtonPresetBlockEXP1`, `jmToggleButtonPresetBlockEXP2`, `jmToggleButtonPresetBlockEXP3`, `jmToggleButtonPresetBlockEXP4`_
- Nick Name for Step #1
  <br>· _`Txt_PresetStepNames1`, `Txt_IAslotStepNames2`_
- Nick Name for Step #2
  <br>· _`Txt_PresetStepNames2`, `Txt_IAslotStepNames3`_
- Nick Name for Step #3
  <br>· _`Txt_PresetStepNames3`, `Txt_IAslotStepNames4`_
- Nick Name for Step #4
  <br>· _`Txt_PresetStepNames4`, `Label2`_
- Each Preset can create a set of Nick-Names which can be assigned to IA-Slots using the Label Parameter. IA-Slots will use the preset-level Label defined here instead of its own Nick-Name
  <br>· _`TextEdit_IALabel1`, `TextEdit_IALabel10`, `TextEdit_IALabel9`, `TextEdit_IALabel8`, `TextEdit_IALabel7`, `TextEdit_IALabel5`_
- if ON, this preset will load its last used Step State. The state is stored with the preset just prior to changing presets.
  <br>· _`jmToggleButtonPresetRememberSteps`_
- OFF: IA-Slot names will follow label definitions or actual IA-Slot Nick Name for Button LCD label display    ON: first 20 IA-Slots of current IA-Map will utilize the label name defined in the preset if it is set. An Empty Label will display the IA-Slot's nickname as normal.
  <br>· _`jmToggleButtonPresetUseMapLabels`_
- When set, the Song's programming will be resent each time a Preset is triggered
  <br>· _`Label1`_
- When set, the Song's programming will be resent each time a Preset is Re-triggered (multiple Presses)
  <br>· _`Label1`_
- Send ALL Songs to the LF+
  <br>· _`CanvasWinMove`_
- Get ALL Songs from the LF+
  <br>· _`LabelGroupName`_
- Block this preset slot from being used in this song
  <br>· _`CanvasDontUse`, `Label1`, `TextEdit_SongLabel5`, `TextField2`_
- Presets assigned in Song Slots 1-12 can have a defined Label to be displayed on the LCD Buttons instead of the Preset Name. If defined (not empty label), the label will be used instead of the Preset's Nick-Name on the LCD buttons assigned to a Preset holder.
  <br>· _`TextEdit_SongLabel4`, `TextEdit_SongLabel3`, `TextEdit_SongLabel2`, `TextEdit_SongLabel1`, `CanvasDontUse`, `TextEdit_SongLabel7`_
- OFF: Do not use Labels if defined, Button LCD will show Preset's NickName.   ON: If a label is defined (not empty), the label name will be used instead of the actual Preset's NickName.
  <br>· _`Label1`_
- IMMEDIATELY will trigger the song as soon as its selected. ARM ONLY will set the song and prepare it to trigger as soon as an ENTER/SELECT button is pressed, a preset is triggered, or any Scroll Event is processed by a button.
  <br>· _`PopupMenuMTC`_
- Enable MTC control as dedicated mode when using this SONG
  <br>· _`Label1`_
- Groups can either send the ON programming of the selected IA-Slot before the BYPASS commands of the others, or vice-versa. This parameter defines the processing order
  <br>· _`PopupGroupMakeBreak`, `Label2`, `GroupBox4`_
- Select the offset flag if the device begins with Preset #001 instead of #000
  <br>· _`Label2`, `jmToggleButtonMidiOffset`_
- Either Select a pre-defined device from the drop down list (which will populate certain fields of the MIDI channel as well), or Type in the Name of the device you have placed on the MIDI channel.

  <br>· _`Label2`_
- When selected, the LF+ will handle all bank changes automatically. Bank commands should not be programmed by user prior to a PC# message.
  <br>· _`TextFieldMaxPresets`, `Label2`, `jmToggleButtonAutoBank`, `jmToggleButtonmsbBank`_
- Defines the maximum preset # usable by this midi channel. Some devices support more then 128 presets. In which case you can enter the highest number. The LF+ will allow programming of PC's for the midi channel to be this value and will automatically handle Bank switching for you if that parameter is selected.
  <br>· _`TextFieldMaxPresets`, `Label2`_
- When selected, the LF+ will handle bank changes using MSB CC#00, LSB CC#32. If not selected, LF+ will send bank changes using standard CC#00 form.
  <br>· _`Label2`, `jmToggleButtonAutoBank`, `jmToggleButtonmsbBank`, `TextFieldMIDIChan`_
- Hover mouse over parameters below for help on what they do
  <br>· _`Label2`_
- Enter the name you want to use for the MIDI device set for this channel. If the device will use SYNC capabilities of the LF+, you MUST select the device from the list- Press the Sync icon to see your options
  <br>· _`TextFieldMIDIChan`, `GroupBox1`_
- If turned OFF, this trigger group will send its commands only when an IA-Slot in the group is changed, otherwise it will send whenever any IA-Slot is triggered
  <br>· _`jmToggleButtonPostSend6`_
- If turned OFF, this trigger group will send its commands only when an IA-Slot in the Post group is changed, otherwise it will send whenever any IA-Slot is triggered
  <br>· _`jmToggleButtonPostSend5`, `jmToggleButtonPostSend4`, `jmToggleButtonPostSend3`, `jmToggleButtonPostSend2`, `jmToggleButtonPostSend1`, `jmToggleButtonPostSend0`_
- the selected IA-Slot in a Post trigger group will trigger when an IA-Slot is triggered in this  trigger group. Set all IA-Slots to this group that should be used in determining the Trigger.
  <br>· _`PopupGroupPostTrigger`, `PushButtonItemSend`_
- Send the Global Settings  to the connected LF+
  <br>· _`PushButtonItemGet`_
- Get the Global Settings from the connected LF+
  <br>· _`CanvasClose`_
- Configuration setting based on # of devices connected to Sync. If 2 devices- both should be set to ON.
  <br>· _`PopupSyncID`_
- Each device connected for SYNC must have a unique Sync ID assigned
  <br>· _`PopupSyncMode`_
- Type of SYNC device if connected to other LF+'s
  <br>· _`PopupMenuExtenderHub`_
- Setting if a Hub device is to be used for Phantom Power, and MIDI IN/OUT and Sync Data to Rig
  <br>· _`Label2`_
- MIDI channel assigned to a LF+ device that will act like an expander for Page button sync'ing. If using the advanced Expansion connectors for integration between multiple LF+'s, SET this parameter set to NOT USED. Do not connect LF+'s via midi cables at the same time you have them connected via Expansion cables- this will cause unpredictable programming execution to your rig.
  <br>· _`LblmainlcdName`_
- Sysex ID. Default is 124
  <br>· _`Label2`_
- MIDI channel assigned to this LF+. It will use this MIDI channel to receive acceptable commands
  <br>· _`Label2`_
- When set, the LF+ will process incoming MIDI commands assigned to its MIDI channel. Otherwise, it will ignore all incoming MIDI commands trying to control the LF+ itself
  <br>· _`jmToggleButtonMIDIthru`_
- Allow incoming Data to be sent to the MIDI-OUT port.
  <br>· _`jmToggleButtonBlockComboMenu`_
- Block the MENU B2+B3 combo buttons from entering the Menu System
  <br>· _`jmToggleButtonBlockComboPage`_
- Block the Page selection button combo sequence (B10 + B11 on Pro+, 12, 12+), (B2 + B6 on the JR+ and MINI)
  <br>· _`jmToggleButtonBlockComboPresetSelect`_
- Block the B1 + B5 Preset change button combo sequence
  <br>· _`Label2`_
- When set, the LF+ will not enter Save/Copy mode when a preset is held for 3+ seconds
  <br>· _`Label2`_
- defines which button # of the active page should be triggered on the first physical button on this LF+. This allows multiple LF+'s to view/trigger different views of the 60 buttons on the current page. (E.g. Set this value to 13, and the LF+ will show buttons 13-24 of the current page on the physical LF+).
  <br>· _`Label2`_
- when ON, Page functions will reset to original order when a Bank Change button is pressed
  <br>· _`jmToggleButtonResetSongChange`_
- when ON, Page functions will reset to original order when a Song or Set-List Change button is pressed
  <br>· _`jmToggleButtonResetPageChange`_
- when ON, Page functions will reset to their original programmed order when a Page Change button is pressed
  <br>· _`Label2`_
- time in (ms) to wait between button presses. To "slow" down the system, increase this number.  normal setting is 40,  value between 0-200. Scroll speed will be effected by this #.
  <br>· _`PopupHoldTime`_
- How long must pass until a 2 function button triggers the second function?
  <br>· _`Label2`_
- as soon as a button is held down for the programmed 2nd Function Time (above), automatically trigger the 2nd function and DO NOT wait for the release of the button. This feature requires BLOCK SAVE/COPY to be turned OFF
  <br>· _`GroupBox4`_
- When Set, The LF+ will stop you from pressing the same Preset more then once.
  <br>· _`jmToggleButtonForceIAResend`_
- Will Resend IA-Slot initial States upon any preset change
  <br>· _`jmToggleButtonBlockOffTriggers`_
- Will send IA-Slot programming Bypass commands when IA-Slot is in the OFF state
  <br>· _`Label2`_
- When Set, The LF+ will not transmit the intial song, preset and IA-Slot Initial States during start up process.
  <br>· _`Separator4`_
- Set to Ignore and the System will power-up in whatever the last used mode was. If you set this parameter, the LF+ will power-up in the mode set by this parameter.
  <br>· _`PopupBootPreset`_
- Set to Ignore and the System will power-up using whatever the last used Preset was (if in Preset Mode). If you set this parameter, the LF+ will power-up and load the preset defined by this parameter. (Preset MODE only).
  <br>· _`Label2`_
- Set to Ignore and the System will power-up with the last used Song (if in Song/Set-List  Mode). If you set this parameter, the LF+ will power-up and load the song defined by this parameter. (works in Song/Set-List mode only).
  <br>· _`Label2`_
- Page the LF+ will default to upon POWER-UP
  <br>· _`PopupBootSetList`_
- Set to Ignore and the System will power-up with the last used Setlist (if in Set-List  Mode). If you set this parameter, the LF+ will power-up and load the setlist defined by this parameter. (works in Set-List mode only).
  <br>· _`GroupBox7`_
- (Show): IA State Changes are displayed     (Delay/Hide): Display the State Change, then replace with song name     (No Show): Do not show IA-Slot State Changes
  <br>· _`Label2`_
- When turned on, LF+ Main display will swap what is displayed on lines #1 and #2.
  <br>· _`jmToggleButtonButton1stLineDisplayType`_
- When turned ON (and show 1st Function name is ON) -- 1st line will show the IA-Slot nick name as all LOWERCASE when the status is OFF/BYPASS, and all UPPERCASE when the status is ON. When turned OFF -- 1st line will show default function NickName as programmed. 
  <br>· _`jmToggleButtonButton2ndLineFunc`_
- When OFF, button LCD screens will show info about the current active function on the button. When this parameter is turned ON, the 2nd line of LCD will show the 2nd function Name
  <br>· _`jmToggleButtonButton2ndLineDisplayType`_
- When turned ON (and show 2nd Function name is ON) -- 2nd line will show the IA-Slot nick name as all LOWERCASE when the status is OFF/BYPASS, and all UPPERCASE when the status is ON. When turned OFF (and show 2nd Function name is ON) -- 2nd line will show default function NickName as programmed.  

  <br>· _`jmToggleButtonBypassasOff`_
- IA-Slots in the Bypass State will show whatever is defined for the OFF state.
  <br>· _`jmToggleButtonClrwhenOff`_
- Button LCD will display nothing when an IA-Slot is OFF and active for the button
  <br>· _`GroupBox5`_
- Tap Tempo Display Type
  <br>· _`Label2`_
- External Sync device used to sync Preset Name, Scene Name, and/or Performance names
  <br>· _`PopupGuitarTapSource`_
- Guitar Tap-Tempo Beat Source
  <br>· _`PopupGuitarTunerSource`_
- Guitar Tuner Source
  <br>· _`Label2`_
- When turned on, LF+ will save the Preset Name loaded from an external device (if successful) after a preset is engaged by user. It will not overwrite the preset name if a valid name was not received from external device.
  <br>· _`jmToggleButtonAllowExtSyncIASave`_
- When turned on, LF+ will save the Current IA-States from the external Sync into the current preset.
  <br>· _`Label2`_
- No blink: USE Status Light only if source is selected.      Auto-Detect: If an IA-Slot of type TAP-TEMPO is currently on a button, it will take over and become the active button.  1-60: force the specific button to blink on the controller.

  <br>· _`jmToggleButtonTapToggle`_
- If turned ON, Tap Tempo IA-Slots (and Auto-tap commands) will toggle between ON/BYPASS programming, otherwise only ON messages are sent during tapping
  <br>· _`jmToggleButtonTunerFullDisplay`_
- Turn ON parameter to block the LF+ from changing all button LCD colors to GREEN when the Guitar is in TUNE.  Tune ON when you prefer TUNER to only show tuning status on the Main LCD. Default will be to turn all LCD BUTTONS GREEN when IN TUNE.
  <br>· _`Label2`_
- When using the Programming Command (Auto Tap Tempo), this parameter will determine how many Taps of the IA-Slot will be generated.

  <br>· _`PopupSyncModelNum`_
- DO NOT use this unless problems exist with a sync device not being detected. FAMC will update these settings with descriptions at a later time.
  <br>· _`Label2`_
- Turn ON to allow the tempo tapped in using a TAP-TEMPO type IA-Slot, to (1) display on the Status light of the LF+ in time, and/or (2) transmit in real-time to the MIDI CLOCK output.  The MIDI CLOCK enable must be turned ON for MIDI CLOCK DATA to output over MIDI. Keep this feature OFF if you plan to Send Auto-Tap-Tempo commands to control the MIDI clock.
  <br>· _`Label2`_
- Turn ON to allow the tempo to sync to the timing derived from the midi clock IN data. While it is not recommended to have MIDI CLOCK turned ON or transmitting, it can be synced. MIDI CLOCK data floods the MIDI path and adds to command latency in some cases. Only allow MIDI CLOCK when necessary.
  <br>· _`Label2`_
- Tap Tempo Status Light Blink Time. How long the tap-tempo Qtr Note light stays on.

  <br>· _`Label2`_
- Allows external devices (Axe-Fx Ultra,Axe-Fx II, KEMPER) to force Presets to update and change on a 1:1 basis. If you change a setting on the EFFECTS Unit, it will update all sync'd IA-Slots automatically.
  <br>· _`Label2`_
- When turned ON, the LF+ will process Auto-Tap-Tempo and MIDI Clock programming commands. This will transmit via MIDI (on a master unit) MIDI CLOCK data. The Source of Midi Clock will be either by programming command, or via tap-tempo if that parameter is turned ON.
  <br>· _`Label2`_
- If an external device enters Tuner on its own, LF+ will jump into Tuner Mode automatically. As an example, Kemper will do this when MIDI volume is set to 0, and will shut off when midi volume is above 0.
  <br>· _`Label2`_
- Color to display when a Preset on Function #1 is not selected but showing
  <br>· _`PopupPresetSelectedColor`_
- Color to display when a Preset on Function #1 is currently selected and showing
  <br>· _`Label2`_
- Color to display when a Preset on Function #2 is currently selected and showing
  <br>· _`Label2`_
- Color to display when a Preset on Function #2 is not selected but showing
  <br>· _`Label2`_
- Select the color displayed on a button when the cooresponding function is the active function
  <br>· _`Label2`, `PopupMenuFunctionColor`, `PushButtonItemSend`_
- Enable this parameter to force the LF+ to remember the last step used before last power down.
  <br>· _`Label2`_
- Turn ON to force the LF+ to remember the last step used before last power down. This IA will continue from the last used STEP when powered back on.
  <br>· _`Label2`_
- Turn ON to force the LF+ to process STEP #1 as soon as a preset is triggered. Turn OFF if you do not want a preset change to automatically trigger STEP #1.  The preset must have RESEND INTIAL IA-Slots turned ON for this parameter to take effect.
  <br>· _`Label2`_
- Send ALL IA-Slots to the LF+
  <br>· _`PushButtonBulkGet`_
- Get ALL IA-Slots from the LF+
  <br>· _`CanvasClose`_
- [ON]  Each channel selection will utilize the "STATE" color selection as indicator.  CHAN A = OFF Color.  CHAN B = ON color Selection.  CHAN C = BYPASS color.   CHAN D = BLOCKED color.
  <br>· _`PopupIASlotBlockedColor`_
- Color to use when the IA-Slot is active on a button and in the BLOCKED state
  <br>· _`Label2`_
- Color to use when the IA-Slot is active on a button and in the BYPASS state
  <br>· _`Label2`_
- Color to use when the IA-Slot is active on a button and in the ON state
  <br>· _`PopupIASlotOffColor`_
- Color to use when the IA-Slot is active on a button and in the OFF state
  <br>· _`PopupPresetLabel`_
- Use a Preset Defined Label instead of the IA-Slot Nick Name. Each Preset can have a different label for the Same IA-Slot. Set a Nick Name on the Preset Screen and then assign the matching # here.
  <br>· _`Label2`_
- Use a Preset Label
  <br>· _`Label2`_
- Connect this IA-Slot to an external device that is Real-Time Sync compatible. Define the external device linkage component. If you do not plan to SYNC your IA's in real-time. DO NOT set this parameter.
  <br>· _`PopupIASlotEffectID`_
- Effect within a Sync'd device to connect to this IA-Slot.  The editor can program the IA-Slot for you with the devices default values once selected if AXE-FX II or less model.   KEMPER: No programming commands need to be defined. LF+ will do it all automatically. Program the ON and OFF color for NON-STOMP effects.    AXE-FX III: No Programming Commands required. LF+ will do everything automatically.
  <br>· _`PopupIASlotEffectIDType`_
- Sync either the Bypass of an Effect, or control the X/Y state of an effect non AXE-FX III+ devices.
  <br>· _`CanvasButtonIASlotGlobalInitState`_
- Initial state when the LF+ is powered on. Only valid when the IA-Slot Global setting is enabled. IA-Slot will otherwise follow the behavior defined at the preset level.
  <br>· _`CanvasButtonIASlotGlobalInitState`_
- Set to ON if this IA-Slot will be a global IA. Global IA's normally keep their "state" while switching between presets. Global IA's have a power-on state defined as their initial state.
  <br>· _`Label2`_
- Defines the behavior of the IA-Slot
  <br>· _`PopupIASlotGroupPostTrigger`_
- Exclusive Post Trigger Group Assignment. By assigning an IA-Slot here, the LF+ will automatically trigger the assigned IA after all other IA's are processed, and it will trigger according to the global settings for the Trigger Group. Multiple IA's can trigger the same Post Trigger Group.
  <br>· _`Label2`_
- Exclusive Post Trigger Group Assignment
  <br>· _`Label2`_
- Defines the Group this IA-Slot will be a part of. IA's within a group can only have one IA ON at the same time. All other IA's will be turned BYPASS. Global settings allow you to control the behavior of the groups bypass processing.
  <br>· _`Label2`_
- AXE-FX III+ device Effects can Sync CHANNEL (A,B,C,D). Use this Parameter to determine the cycle you prefer for this Effect. Triggering this IA-Slot will cycle from CHAN A->the last CHAN you determined allowed.
  <br>· _`Label2`_
- Device connected to this IA-Slot for sync purposes
  <br>· _`Label2`_
- Turn ON to block STEP TYPE IA-SLOTs from automatically switching channels incrementally when triggered. The user can thus program individual steps with full control on each step. The IA-Slot will sync the current CHAN read in and trigger that STEP. So CHAN A = STEP 1, CHAN B = STEP 2, CHAN C = STEP 3, CHAN 4 = STEP 4. You can use the [EXT DEV  CHANNEL CHANGE] programming command to force specific channels
  <br>· _`LabelEffectConfigData1a`_
- DEFAULT [OFF]:  Turn [ON] to Allow this IA-Slot to send its programming when a PRESET is: [SYNC EXTERN IA-SLOTS = ON] and [RESEND IA-SLOT INITIAL STATES = ON].  Normally a Preset will not send the programming commands of a sync'd IA-Slot since it expects the state to change in real-time.  OFF makes this compatible with V5.x or below behavior.
  <br>· _`LabelChanColor1`_
- If set, this parameter will tell this set-list which Song Slot is the last used Song. Once a Song-UP command reaches the last song, it will no longer scroll up.
  <br>· _`PopupMenuCycleType`_
- Defines Type of Cycle when Song UP is pressed at the end of the set-list: (1) Stop- stop scrolling, (2) Start Of List - After last song in set, cycle to first song.
  <br>· _`Label1`_
- Send ALL Set-Lists to the LF+
  <br>· _`PushButtonBulkGet`_
- Get ALL Set-Lists from the LF+
  <br>· _`CanvasClose`_
- If using the Button Start Value (Global settings), or the Button Start programming command, this value will show the buttons above on the physical controller
  <br>· _`RoundRectangleShadow`, `CanvasVirtualPage`, `Label26`, `Label25`, `Canvas9`, `GroupBoxHEADER`_
- Set the Color of Status Led #1 when this page is active
  <br>· _`PopupMenuButtonAssign`_
- by default, Button #2 + #3 held together turn on the menu system (unless blocked in global settings). You can override this to trigger any BUTTON defined on the current page.
  <br>· _`PopupMenuForceIAMap`_
- By default, Pages will not alter the current IA-MAP (as set by presets). You can override Preset-Level IA-MAP selection using this parameter. Be careful as IA-Maps maybe out-of-sync with Preset Initial States when forcing Maps at a Page level.
  <br>· _`PopupMenuForceModeChange`_
- by default, loading pages will NOT change the operating Mode. You can force a change to PRESET, SONG or SET-LIST mode with this parameter
  <br>· _`label1`_
- To override the Global Setting for the color used by PRESET functions on a button. Please note when using Page level color, both Function #1 and #2 preset holders will appear the same color.
  <br>· _`PopupPresetSelectedColor`, `label1`_
- Set this Parameter if you want to force an IA-Slots programming to trigger when a page is loaded. This allows a page to send commands each time its loaded
  <br>· _`label1`, `PopupMenuReleaseType`_
- forces all buttons on this page to allow double-tap button presses to switch between Function #1 and Function #2
  <br>· _`label1`_
- When turned On, IA-Slots will dynamically take over a button based on the current status of the IA-Slot. Function #1 IA-Slots in the ON state will always be active when page loads. If function #1 IA-Slots are bypassed or OFF, then Function #2 IA-Slots will become the initial active function if they are set to ON state
  <br>· _`label1`_
- show all 60 buttons of the page. Programming and viewing all 60 buttons can be done on this screen
  <br>· _`label1`_
- Press this representation to edit PAGE buttons (49-60). When pressed, The Buttons to the Left are how they will map onto the actual buttons of the LF+. To use this virtual page layout, make sure to set the preset parameter (PAGE BUTTON START) to 49. 
  <br>· _`CanvasVirtualPage`_
- Press this representation to edit PAGE buttons (37-48). When pressed, The Buttons to the Left are how they will map onto the actual buttons of the LF+. To use this virtual page layout, make sure to set the preset parameter (PAGE BUTTON START) to 37. 
  <br>· _`Label28`_
- Press this representation to edit PAGE buttons (1-12). When pressed, The Buttons to the Left are how they will map onto the actual buttons of the LF+. Make sure to set the preset parameter (PAGE BUTTON START) to 1 (which is the default value). 
  <br>· _`CanvasVirtualPage`_
- Press this representation to edit PAGE buttons (25-36). When pressed, The Buttons to the Left are how they will map onto the actual buttons of the LF+. To use this virtual page layout, make sure to set the preset parameter (PAGE BUTTON START) to 25. 
  <br>· _`Label27`_
- Button Number within the PAGE definition. This number defines the actual button with the page you are editing. 
  <br>· _`LabelStartVal`, `Canvas21`, `RoundRectangle1`_
- Pick a button to edit. Type it in and press return
  <br>· _`Label8`_
- Press LEFT mouse button to select the TYPE of button function
  <br>· _`CanvasDownArrow`_
- Press the LEFT mouse button to select the function parameter (setting)
  <br>· _`CanvasDownArrow`_
- When set, this button will trigger a change to the system if a BANK, PRESET, PAGE, SONG, SET-LIST   UP or DOWN event was pressed prior.  For instance, if a Bank UP command was triggered and this button has process scrolls set, then the system will process the bank up immediately.
  <br>· _`lblPageButtonFuncLabel`, `jmToggleButtonButtonScroll2`, `jmToggleButtonButtonScroll1`_
- When set, this button will trigger a change to the system if a BANK, PRESET, PAGE, SONG, SET-LIST   UP or DOWN scrolling event was pressed prior.  For instance, if a Bank UP command was triggered and this button has process scrolls set, then the system will process the bank up immediately.
  <br>· _`RoundRectangle2`, `PopupMenuTriggerType`_
- Double tap, or long press of a button can either toggle (change active position), trigger (execute the commands of the currently 2nd function), or both (changae positions and trigger): 
Toggle/Trigger = Change active button function from Function #2 to #1, and then Trigger what is now toggled to Function #1.
Toggle Only = switch the functions around, so the current #2 is now #1. 
Trigger Only = Trigger Function #2, but do not toggle it to the #1 position.
  <br>· _`jmToggleButtonButtonDoubleTap`_
- The current button will allow double-tap functionality to switch between Function #1 and Function #2. Similar to double-clicking a mouse on computer. The behavior of the double-tap even is dictated by the Trigger Type parameter.
  <br>· _`PopupMenuIAMapReference`_
- Immediate Processing will trigger function #1 as soon as the button is pressed. If a second function is defined, it will wait. Wait for release will wait for the button to be released before triggering.
  <br>· _`label1`_
- Press this representation to edit PAGE buttons (13-24). When pressed, The Buttons to the Left are how they will map onto the actual buttons of the LF+. To use this virtual page layout, make sure to set the preset parameter (PAGE BUTTON START) to 13. 

  <br>· _`PushButtonItemSend`_
- Send ALL Pages to the LF+
  <br>· _`PushButtonBulkGet`_
- Get ALL Pages from the LF+
  <br>· _`CanvasClose`_
- Send ALL IA-Maps to the LF+
  <br>· _`PushButtonBulkSend`_
- Get ALL IA-Maps from the LF+
  <br>· _`PushButtonBulkGet`_
- Enter hex values using &h, example (F7 = &hF7)
Enter Dec values in the range of 0-127
  <br>· _`lblHEX`, `txtSysexData`, `Label1`_
- Send ALL Sysex to the LF+
  <br>· _`PushButtonBulkGet`_
- Get ALL Sysex from the LF+
  <br>· _`CanvasClose`_
- send the sysex message in this link prior to sending this current sysex message.
  <br>· _`PopupMenuPostLinkID`_
- send the sysex message in this link after this current sysex message is finished sending.
  <br>· _`Label1`_
- Rescan for new midi MIDI hardware
  <br>· _`PushButtonRescan`_
- add an effect to the current device definition
  <br>· _`PushButtonAddEffect`_
- remove the currently selected effect and move all the effects below up it
  <br>· _`PushButtonDeleteEffect`_
- assign MIDI chan to All effects
  <br>· _`PushButtonAssignMidiChan`_
- Select a MIDI Chan # to assign to all effects within this device definition. It is not necessary that all effects use the same device chan. You can mix and match actual devices into one definition
  <br>· _`PopupMenuMIDIchan`_
- Enter a first CC# to auto-program. This will save us a lot of time in data entry
  <br>· _`PopupMenuCCStart`_
- The last CC# you want to autoprogram to save time
  <br>· _`PopupMenuCCEnd`_
- Automatically create Effect CC slot definitions from the Start to the End definition-- Append to the end of the current Effects defined.
  <br>· _`PushButtonAutoProgram`_
- Click to Add a new device to our definitions locally
  <br>· _`BevelButtonAdd`_
- delete the currently selected and active device definition
  <br>· _`BevelButtonDelete`_
- Names of Devices we have defined and active with the editor
  <br>· _`PopupMenuDevices`_
- Current device name, enter a new name to rename or create a new device defintion
  <br>· _`TextFieldDeviceName`_
- Get a list of all devices saved online from all users. Select matching devices of interest. Import and delete if not usable. Or use as a starting point to edit for your own needs
  <br>· _`BevelButtonSelectImportWeb`_
- Import the Devices Selected in the list. This will import into our Active Device Definitions so we can use / modify.
  <br>· _`PushButtonImport`_
- Select a file to import a file you previously saved
  <br>· _`BevelButtonSelectImport`_
- Save the selected devices to a file on disk
  <br>· _`BevelButtonExportToFile`_
- Save the selected definition online so others can see/share
  <br>· _`BevelButtonExportToWeb`_
- Delete the selected devices
  <br>· _`BevelButtonDeleteSelectedFromWeb`_
- Get a list of all devices you stored online
  <br>· _`BevelButtonLoadFromWeb`_
- Used for Device Definition Files
  <br>· _`TextFieldAuthor`, `TextFieldHWSN`, `LabelSN`_
- Enable this parameter if you are having USB connection problems. If this does not solve your issue, please contact FAMC.
  <br>· _`jmToggleButtonAllowLooseConnection`_
- Will try to connect to your LF+ using the last settings that were successful to speed up the process
  <br>· _`jmToggleButtonUseLastPort`, `jmToggleButtonBlockMouseWheel`_
- Will not remember any window size changes you make
  <br>· _`TextFieldUserDirectory`, `Label13`, `TextFieldBackupDirectory`, `CheckBoxShowAtStartup`, `PushButtonDownload`_
- Click this directory to set another location to store your programming files
  <br>· _`TextFieldUserDirectory`, `TextFieldBackupDirectory`_
- When "Locked", the user storage directory will NOT change when you select a different area to save or open a file. The default location will not change.  When unlocked, the last used directory will be stored and used thereafter.
  <br>· _`jmToggleButtonLockDirectory`_
- Advanced: use if you have trouble connecting to LF+ via USB
  <br>· _`Label10`, `Label14`, `jmToggleButtonBlockDisconnectAutoExit`, `PushButtonCancel`_
- If set, the editor will not tell the LF+ to exit editor mode when you disconnect via the disconnect switch.
  <br>· _`jmToggleButtonBlockDisconnectAutoExit`_
- Define the Type of Pedal Function for this expression pedal port
  <br>· _`PopupMIDIChan`_
- MIDI Chan used to transmit CC related expression pedal values. This is only used for MIDI command based Expression pedal types.
  <br>· _`PopupExpData`_
- Either the CC#, IA-Slot# or Button # based on Expression Pedal Type
  <br>· _`PopupExpData2`_
- Button #2 to trigger on a two button expression pedal type
  <br>· _`Label2`_
- Value 1-180 -- IA Slot to Trigger when entering full HEEL position
  <br>· _`PopupMenuTriggerToe`_
- Value 1-180 -- IA Slot to Trigger when entering full TOE position
  <br>· _`Label2`_
- By default, Each time a Preset is loaded, all Expression Pedal Programming Parameters default back to Global Settings. Setting this parameter will allow changes made to stay without resetting
  <br>· _`jmToggleButtonAutoCalibrate`_
- When Turned On, this expression pedal port will calibrate upon power-up after the pedal (or buttons) are first used.  The pedal or buttons must be installed prior to power-up each time.
  <br>· _`jmToggleButtonForceZipper`, `jmToggleButtonJitterBlockHeelToe`, `Label2`_
- Continous Controllers: Some effects unit require sequential sweeps during changes to sound proper. If zipper is turned ON, then if pedal values change quick are made (say from a value of 0 to 100, then the LF+ will send MIDI commands for all values in between (0,1,2,3...100). Given MIDI latency you may experience slight delays while moving pedals that are zippered.
  <br>· _`jmToggleButtonJitterBlockHeelToe`, `jmToggleExpBlockPresetReset`_
- When turned on, this expression pedal will process all changes when between 0/1 and 126/127 midi values. As POT's in expression pedals age, get dirty, and some loose stiffness or integrity. They fail to properly provide detailed pedal changes. This parameter can compensate. The downside is that a spuradic or continuous stream of midi data could be transmitted. Test your pedal using this parameter if not getting to 0 or 127. Sensitivity level must not be set to ignore for this to work.
  <br>· _`Label2`_
- Default sensitivity will properly work with most pedals that have semiaccurate POT's installed. Experiment with the settings (HI or LO) should you either not get enough values transmitting, or too many repeat values transmitting. Set to IGNORE to stop filtering completely. Ignore will appear to constantly transmit values for pedals with moderate quality POT's installed.
  <br>· _`Separator1`, `Separator2`, `Label2`_
- Value 0-127 -- Min value when in heel position
  <br>· _`Label2`_
- Value 0-127 -- Max value when in toe position
  <br>· _`Label2`_
- When ON, LF+ will process the pedal with twice the accuracy. Use with good quality pedals only. Test sensitivity levels for best results. you must re-calibrate (if not using auto-calibrate) after switching HI-RES mode on or off.
  <br>· _`Label2`, `GroupBox11`_
- Reset the expression pedal calibration data to start over
  <br>· _`PushButtonReset`, `RoundRectangleLiveStatus`_
- Will only process the command filter parameters to find items.
  <br>· _`PushButtonFindCommands`_
- IF ON:  Will match on the programming command only.
  <br>· _`jmToggleButtonCommandOnly`_
- IF ON:  If any MIDI command is found matching the search criteria MIDI CHAN, then include it. Ignore all other search criteria. You can select any MIDI message in the search, just select the MIDI chan you are looking for. This will show all places that this midi device is used.
  <br>· _`jmToggleButtonSearchMIDIChan`_
- IF ON:  If a matching command is found in the IA-Slot ON messages, then don't report it again if found in the Bypass Messages.
  <br>· _`jmToggleButtonShowIAOnOnly`_
- If ON: searching for CC# MIDI command will ignore the Data Value and show All items that trigger the MIDI CHAN/CC#.  If searching for PC# MIDI command, it will only look for the MIDI chan and ignore the PC#.
  <br>· _`jmToggleButtonIgnoreValue`, `jmToggleButtonAutoIncriment`_
- IF ON:  Will Search All presets for Matching Criteria
  <br>· _`jmToggleButtonFilter_Presets`, `jmToggleButtonFilter_Songs`, `jmToggleButtonFilter_SetLists`, `jmToggleButtonFilter_IASlots`, `jmToggleButtonFilter_Pages`, `jmToggleButtonFilter_Sysex`_
- Starting Search Item
  <br>· _`textfield_Min`_
- Ending Search Item
  <br>· _`textfield_Max`_
- Will reset all filter criteria to default values
  <br>· _`jmToggleButtonRESET`_
- Will only process the Component filter parameters to find items.
  <br>· _`PushButtonFindRecords`_
- if a search result item contains programming (Presets, IA-On, IA-Bypass, Songs), then include the programming details.

  <br>· _`Checkbox_IncludePresetIAstates`, `Checkbox_IncludePresetLabels`, `Checkbox_IncludeProgramming`_
- if a search result item contains  songs, include the song list detail for each setlist. 
  <br>· _`Checkbox_IncludeSetlistSongs`_
- if a search result item contains  songs, include the preset list detail for each song. 
  <br>· _`Checkbox_IncludeSongPresets`_
- if a search result item is a Preset, IA-Slot, Song, Set-List, IA-Slots, Page, or Sysex msg, then it will include the full options/parameter settings.
  <br>· _`Checkbox_IncludeSettings`_
