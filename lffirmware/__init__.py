"""LF+ Firmware Loader — a standalone, safety-first firmware flasher for FAMC Liquid Foot+
controllers, built for recovering bricked units via the device's documented MIDI firmware-wait
mode. Separate from the editor on purpose: flashing stays out of the main app until this tool
has enough field validation. See docs/FIRMWARE_LOADER.md."""

__version__ = "0.1.0b1"
